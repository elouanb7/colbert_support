<div class="modal fade" tabindex="-1" role="dialog" id="alert{{ ticket.id }}">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <div class="modal-header bg-danger">
                <h5 class="modal-title">Attention !!!</h5>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-body">
                <p>Vous êtes sur le point de supprimer un ticket ! Confirmez-vous la suppression ?</p>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-info" data-dismiss="modal">Annuler</button>
                <a href="{{ path('del_panne',{'id': ticket.id }) }}" class="btn btn-danger">Supprimer</a>
            </div>
        </div>
    </div>
</div>
